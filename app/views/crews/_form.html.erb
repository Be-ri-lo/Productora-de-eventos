<%= form_with(model: [@band, crew], local: true) do |form| %>
  <% if crew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crew.errors.count, "error") %> prohibited this crew from being saved:</h2>

      <ul>
      <% crew.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :members_name %>
    <%= form.text_field :members_name %>
  </div>

  <div class="field">
    <%= form.label :band_id %>
    <%= form.text_field :band_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
